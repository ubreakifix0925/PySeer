# ============================================================
# 默认忽略所有内容, 仅保留"运行项目必须"的文件.
# 运行必需 = 程序文件(app/) + 说明文档(README.md, docs/) + 本文件 + 一键脚本(start.sh/deploy.sh).
# 其余(运行时下载/生成的资源、逆向参考资料、分析工具与产物、缓存、凭据、日志、字节码、
#     PROJECT_NOTES.md 交接笔记、可选本地扩展 app/multi.py 等)一律忽略, 不上传 GitHub.
# ============================================================

*

# ---- 运行必需：程序文件 (app/) ----
!README.md
!start.sh
!deploy.sh

!app/
!app/**
!app/seer/
!app/seer/**
!app/cmdmap.json
!app/requirements.txt

# ---- 忽略：用户脚本目录 (不入库; 即 WebUI "脚本"页的本地脚本目录) ----
app/scripts/
app/scripts/**

# ---- 忽略：可选本地扩展 (本地专用, 不上传 GitHub; webui.py 有加载 hook) ----
app/multi.py

# ---- 文档 ----
!docs/
!docs/**

# ---- 仍要忽略的运行产物/字节码 (即便目录保留) ----
*.pyc
**/__pycache__/
