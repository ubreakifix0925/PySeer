# ============================================================
# 默认忽略所有内容, 仅保留"运行项目必须"的文件.
# 运行必需 = 程序文件(app/) + 说明文档(README.md, docs/) + 本文件 + 一键脚本(start.sh/deploy.sh).
# 其余(运行时下载/生成的资源、逆向参考资料、分析工具与产物、缓存、凭据、日志、字节码、
#     PROJECT_NOTES.md 交接笔记、可选本地扩展 app/multi.py 等)一律忽略, 不上传 GitHub.
# ============================================================

*

# ---- 运行必需：程序文件 (app/) ----
!README.md
!start.sh
!deploy.sh

!app/
!app/**
!app/seer/
!app/seer/**
!app/cmdmap.json
!app/requirements.txt

# ---- 忽略：用户脚本目录 (不入库; 即 WebUI "脚本"页的本地脚本目录) ----
app/scripts/
app/scripts/**

# ---- 忽略：可选本地扩展 (本地专用, 不上传 GitHub; webui.py 有加载 hook) ----
app/multi.py

# ---- 文档 ----
!docs/
!docs/**

# ---- 功能性游戏数据基线 (让 git clone 后即可离线显示精灵名/属性/技能/魂印) ----
# 这些是 assets_updater 从游戏包解析出的**功能数据**(JSON, 跨平台); 头像/图标(data/head,
# data/effecticon)与运行时文件(data/webui_*)仍忽略, 由启动时下载生成. vendor/(UnityPy)含
# 平台相关编译产物(.so), 不做 git 提交, 由 assets_updater 按当前平台自动 pip 安装到 vendor/.
!data/
!data/petbook.json
!data/pet_attr.json
!data/skills.json
!data/soulmarks.json
!data/item_names.json
!data/.petbook_state.json
!data/.pet_attr_state.json
!data/.skills_state.json
!data/.soulmarks_state.json
!data/.item_names_state.json

# ---- 仍要忽略的运行产物/字节码 (即便目录保留) ----
*.pyc
**/__pycache__/
